getgenv()._SETTINGS_ = {
    fps = "50", -- sets fps cap to 50
    lowgfx = true, -- set to false if you dont wanna lower cpu % {recommended to keep on}
}
loadstring(game:HttpGet("https://github.com/15rih/ModifiedEclipse/blob/main/source.lua"))()
